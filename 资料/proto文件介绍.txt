message 定义protobuf中的消息类型 消息就是需要传递的数据格式的定义
类似go中的struct
required字段是必填字段 在protobuf3中被删除
opitional是可选字段 protobuf3中没有这连个说明了 都默认为opitional
repeate消息体中的可重复字段，重复的值的顺序会被保留在go中重复的会被定义成切片
在消息体的定义中，每个字段都有一个唯一的标识号 范围是1-2……29-1

消息还可以嵌套使用 调的时候只要点进去（.）就行了

服务里面可以定义函数
函数就是rpc打头 有名字参数返回值

这个文件就是一个约束,不用在这实现